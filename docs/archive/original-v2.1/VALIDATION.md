# v2.1 文档整合与包检查记录

日期：2026-09-06。执行环境是当前Linux容器，不是用户的Windows电脑。

## 本次实际执行

- 重新运行`python -m unittest discover -s tests -v`：**原有41项参考测试全部通过**，结果见`TEST_RESULTS.txt`。没有把新增业务要求算成已经实现的测试。
- 全部Python文件通过AST解析与compileall语法检查。
- 全部YAML/JSON配置可以解析。
- `list_cameras.py --help`与`smoke_pose.py --help`实际运行成功。
- Markdown代码围栏配对、主规范0—19节连续、引用编号有对应来源条目。
- 对照v2.0原包，13个Python、配置及依赖文件字节一致；本次没有修改应用逻辑或增加训练依赖。
- 更新主规范与配套Codex说明，资源分级、模式范围、版本说明相互一致；生成新内容哈希。

## 41项参考测试覆盖

13项几何/有效观察时间；21项设备候选匹配、坐标映射和运行上下文隔离；7项配置范围约束。

这些测试不是完整CameraManager、GUI、康复状态机、安全引擎或新质量字段的实现验证。新增的逐指标有效性、多标签问题、调试导出、固定回归等是给Codex的开发与验收要求。

## 本次没有做

未连接实际Windows相机、USB设备、GPU或雷达；没有测试热插拔、真实姿态推理、帧率、端到端延迟、真实动作准确性或患者使用。
未运行Sports2D、STGCN-rehab或旧跌倒模型；未下载研究权重、REHAB24-6等数据集。
未开发完整桌面程序、新增业务逻辑或数据库迁移代码。原有脚本仍只是单路输入/姿态接入检查。

新包只是更新后的任务书、参考配置与既有检查脚本。不能把“41项通过”宣传成四场景产品验收、临床有效性或跌倒监护可靠性通过。
